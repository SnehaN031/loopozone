/**
 * Utility to generate a static OTP for development/demo stage.
 * Can later be replaced with secure dynamic random code generation.
 */
const generateOTP = () => {
  return '654321';
};

module.exports = generateOTP;
